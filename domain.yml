version: "3.1"

intents:
  - bot_challenge
  - ask_course_name
  - nlu_fallback
  - chit_chat_professor

entities:
  - course_code

slots:
  course_code:
    type: text
    mappings:
      - type: from_entity
        entity: course_code

responses:

  utter_chit_chat_professor:
    - text: "สุดหล่อ"

  utter_iamabot:
    - text: "I am a bot, powered by Rasa."


actions:
  - action_query_typhoon
  - action_fetch_course_name


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
